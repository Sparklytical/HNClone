import React from 'react';

import { Animation } from './LoaderStyles';

const Loader = () => (
  <Animation>
    <span>.</span>
    <span>.</span>
    <span>.</span>
  </Animation>
);

export default Loader;
